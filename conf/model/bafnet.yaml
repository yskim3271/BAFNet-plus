model_lib: bafnet
model_class: BAFNet
input_type: acs+bcs

param:
  win_len: 400
  hop_len: 100
  fft_len: 400
  conv_depth: 4
  conv_channels: 16
  conv_kernel_size: 7

  checkpoint_mapping:
  checkpoint_masking: